import { Container } from 'theme-ui'
import {
    BrowserView,
    MobileView,
    isBrowser,
    isMobile
  } from "react-device-detect";

export const ConditionalContainer = props => (
    <>

    </>
)
